# Fridge Notes
Fridge Notes is a shared digital whiteboard web app running on Express.js and Socket.IO. 

# How to run
Setting up a local server is simple. Clone the project and run:

`npm install`

`node server.js`
